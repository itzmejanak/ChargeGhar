1. /api/app/config/public need to add into Admin site by remaining /api/app/admin/config and make CRUB for it

2. /api/app/countries/search (remove it)
3. /api/app/init-data ( remove it )
4. /api/app/media/uploads ( need to add on admin site by remaining it to /api/app/admin/media/uploads
5. /api/app/media/uploads/{upload_id} ( need to add on admin site by remaining it to /api/app/admin/media/uploads/{upload_id})
6. /api/app/updates/since/{version} (remove it)
7. /api/app/versionCheck ( remove it )
8. /api/auth/debug/headers ( remove it )

9. /api/referrals/validate ( remove it but the validation logic should handle first before claim by /api/referrals/claim)

10. /api/promotions/coupons/validate ( remove it but the validation logic should handle first before claim by /api/promotions/coupons/apply)

11. /api/content/search ( remove it )



